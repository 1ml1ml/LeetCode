#pragma once

#include <vector>
#include <algorithm>

class CSolution
{
public:
	int threeSumClosest(std::vector<int>& vecNum, int target) 
	{
		//≈≈–Ú
		std::sort(vecNum.begin(), vecNum.end());

		
	}
};
