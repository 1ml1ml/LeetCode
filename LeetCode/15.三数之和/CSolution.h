#pragma once

#include <algorithm>
#include <vector>
#include <map>
#include <set>

class CSolution 
{
public:
	std::vector<std::vector<int>> threeSum(std::vector<int>& vecNum)
	{
		return { {} };
	}
};
